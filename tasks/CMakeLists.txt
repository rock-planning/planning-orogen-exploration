# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(explorationTaskLib)
ADD_LIBRARY(${EXPLORATION_TASKLIB_NAME} SHARED 
    ${EXPLORATION_TASKLIB_SOURCES})
add_dependencies(${EXPLORATION_TASKLIB_NAME}
    regen-typekit)

TARGET_LINK_LIBRARIES(${EXPLORATION_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${EXPLORATION_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${EXPLORATION_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${EXPLORATION_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${EXPLORATION_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${EXPLORATION_TASKLIB_HEADERS}
    DESTINATION include/orocos/exploration)

